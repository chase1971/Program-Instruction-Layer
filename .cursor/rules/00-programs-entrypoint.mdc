---
description: Programs tree entrypoint — superseded by AGENTS.md, which loads every session
alwaysApply: false
---

# Programs AI entrypoint

Workspace root: `C:\Users\chase\Documents\Programs\`

**This rule is no longer always-on.** Everything it used to restate — required reads, hard
rules, ask-before-assume — now lives once in **`AGENTS.md`**, which every tool loads every
session. Read that instead of reintroducing a second copy here.

## Required reads (in order)

1. **`AGENTS.md`** (root) — already in context; the always-on rules
2. **`APP_LOCATIONS.md`** — where every app lives; read before searching the tree
3. **`cursor-patterns/CODING_STANDARDS.md`** — before writing or editing code
4. App **`CLAUDE.md`** + **`guidelines/Guidelines.md`** when inside a specific app
