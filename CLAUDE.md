@AGENTS.md

## Claude Code

This repo uses `@AGENTS.md` as the shared baseline. App-specific guidance lives in each app's `CLAUDE.md` (index of that app's `.cursor/rules/*.mdc` contracts). Claude Code does not auto-load `.mdc` rules — read the app's `CLAUDE.md` when working inside one.
