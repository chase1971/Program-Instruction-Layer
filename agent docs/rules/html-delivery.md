# HTML delivery to Chase (clickable chat links)

> **Rung 5 — on demand.** The always-on summary is root `AGENTS.md` § HTML pages Chase can click from chat; this
> file is the detail behind it. Read it when that section is not enough.
> **What it covers:** Clickable HTML for Chase — serve docs/ on 8765, http link in chat only. Read when Chase asks to link/open HTML, infographic, comparison, or when editing docs/**/*.html. Never C:\ paths or .bat openers.
>
> *Was `.cursor/rules/html-infographic-delivery.mdc` until 2026-08-02. Moved because `.cursor/rules/*.mdc`
> only load when a matching file is open in a Cursor editor tab — which is not how Chase works.
> AGENTS.md links this file by path instead, so every tool can reach it.*

---

**Not always-on** — the always-on half is only the *never* (no `C:\`, no `file:///`, no `.bat`),
because pasting a file path is the default instinct. Everything below is procedure: it only
matters once you've decided to build a page, and by then you know to read this.

## Where files actually go

**There is no `Programs/docs/` folder.** It has never existed. Earlier versions of this doc and
of `AGENTS.md` both said to write there — corrected 2026-08-05.

| Kind of page | Folder | Maintained? |
|---|---|---|
| **Explains how the instruction layer is organized** | `agent docs/instructional-layer-htmls/` | **Yes** — update it when the structure changes |
| **One-off "show me what this does"** | `agent docs/scratch/` | **No** — add a dated banner; assume it goes stale |
| **Teaching material** — exam maps, review maps, handouts | `School Scrips/School documents/` | Its own repo |
| Generated by a script | wherever the script writes | Never edit by hand |

**The front door is `agent docs/pages.html`**, served at **`http://127.0.0.1:8765/pages.html`**.
Give each page a `← All pages` link back, and make that link the **absolute** `/pages.html`.

> **Never link the doc index as `/` or `/index.html`.** Both resolve to
> `School Scrips/School documents/index.html` (**Pearson Exam Maps**), because School documents is
> the first served root and wins every name collision. That shadowing silently broke 39 back-links
> across 29 pages until 2026-09-08 — every "← All pages" button landed on the exam maps.
> The agent-docs front door is called `pages.html` precisely so it cannot collide.

> **`index.html` is GENERATED — never hand-edit it.** It is rebuilt by
> `node scripts/build-doc-index.js`, which scans the served roots and merges
> `agent docs/page-manifest.json` for grouping and descriptions. A hand-edit is lost on the next
> rebuild.
>
> **To add a page:** put a `match` entry (and ideally a `descriptions` entry) in
> `page-manifest.json`, then run the build. A page you don't file still appears — under
> **Unsorted** — so nothing goes missing, but filing it is the courtesy.
>
> Pins and open/closed sections are the reader's own state, kept in `localStorage`. They survive a
> rebuild; do not try to encode them in the manifest.

`scripts/serve-programs-docs.js` serves **three** roots, checked in this order:
`School Scrips/School documents/` → `Programs/docs/` (absent) → `agent docs/`.
School documents wins ties — that is why the live `exam2-review-map.html` is the one there,
not the 1 KB stub in `agent docs/`. **Subfolders work**, so
`/instructional-layer-htmls/foo.html` resolves.

## Trigger phrases

| Chase might say | Do this |
|---|---|
| "link it" / "give me a link" / "make me an HTML page I can click" | Build → serve → http link |
| "HTML" + report / map / comparison / infographic | Same |
| "clickable" / "open from here" / "open from chat" | Same |

**Never** paste `C:\Users\...`, `file:///`, or a bare relative path as the only link.

## Workflow

1. Self-contained `.html` (inline CSS, no external fetches) in the folder from the table above.
2. `node scripts/serve-programs-docs.js` if port 8765 isn't up (background OK, no browser).
3. Verify: `Invoke-WebRequest http://127.0.0.1:8765/<file>.html -UseBasicParsing` — expect 200.
4. Reply with **one markdown link**: `http://127.0.0.1:8765/<file>.html`.
5. Do **not** open the browser yourself — root `AGENTS.md` § Never put anything on Chase's screen.
6. Dwell-friendly: big click targets, whole cards clickable, no hover-only affordances.

## Non-negotiables

| Rule | Bug it prevents |
|------|-----------------|
| Never `C:\...` / `file:///` in chat | `%5C` broken link |
| Never `.bat` delivery | Chase won't browse to double-click |
| Port **8765** + `serve-programs-docs.js` | Broken bookmarkable URLs |

## Reader comments — Chase annotating a page, agent reading it back

Added 2026-09-11. A served page can collect Chase's own comments and leave them somewhere an
agent reads directly, so he can annotate a long report instead of re-typing points into chat.

**The one write the server allows.** `scripts/serve-programs-docs.js` accepts
`POST /__comments/<page path>` and writes the JSON body to **`<page>.comments.json` beside the
page** — e.g. `scratch/portal-risk-review.html` → `agent docs/scratch/portal-risk-review.comments.json`.
Nothing else is writable: non-`.html` targets, pages that don't exist, and path traversal all 404,
and `GET` on the endpoint is 405. Reading the file back is just the static server (it serves `.json`).

**When Chase says "read my comments on X"** → read `<that page>.comments.json`. Shape:

```json
{ "page": "scratch/foo.html", "updated": "<iso>",
  "comments": { "<card id>": {
      "title": "<the heading he commented on>",
      "thread": [ { "text": "…", "updated": "<iso>" } ],
      "draft": "<typed but not yet posted — answer it too>"
  } } }
```

Each entry carries the heading text, so the file stands alone — you do not need to open the HTML to
know what a comment refers to. A `draft` is text he typed without pressing Post; treat it as a real
comment. Pages written before 2026-09-11 used a flat `"text"` per card instead of `thread`; the
loader migrates that shape on read, so either may appear on disk.

**Answering him happens in the page, not in chat.** Replies live in the page's own `REPLIES` object,
keyed by card id, as an array of HTML strings: index *N* answers his post *N*, so a card renders
post 0, reply 0, post 1, reply 1 … To answer a new comment, append a string to that card's array —
never rewrite an existing reply, or the thread stops matching what he remembers reading. The page
shows a "waiting for a reply" line on any card where his posts outnumber the replies, and counts
those at the top, which is how you (and he) see what is still unanswered.

**To put comment boxes on a new page:** copy the pattern from
`agent docs/scratch/portal-risk-review.html` — stamp `data-comment-id` (a stable slug) and
`data-comment-title` on each card, then reuse its `<script>` block with `PAGE`/`DATA_URL` changed.
The script injects the boxes, so cards stay clean and a new card gets a box for free.
**Do not build a second save path** (no localStorage-only notes, no copy-to-clipboard export) —
browser-only storage cannot be read by an agent, which is the entire point of this one.

**Ids must stay stable across rewrites.** The comments file keys off `data-comment-id`. If you
regenerate a page, keep the existing ids or the comments detach from their points.

**The server must be running for a save to land.** The page shows a red "could not save" line and
keeps the text in the box, so nothing is lost — but a restart of `serve-programs-docs.js` is needed
after editing the server itself.

## Comparison layouts

Side-by-side homework/review (or similar): copy structure from **`School Scrips/School documents/exam2-review-map.html`** — do not invent a new always-on rule.

## Session tracking log (write-only)

**`agent docs/session-tracking-log.html`** — one row per task bump; expand for navigation path.  
**`agent docs/session-metrics-log.html`** — greps/files per finalized session.

Both generated by `scripts/append-session-scorecard.js`. Append via `--bump-file` / `--finalize-file` — **never read these HTML files into context** (Chase's dashboard).  
Procedure: `agent docs/SESSION_TRACKING.md` (bumps) · `agent docs/SESSION_METRICS.md` (finalize).
